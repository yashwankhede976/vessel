# Scripts

Developer and operational helper scripts (setup, running services, data tasks).

Current scripts:

- `dev-backend.sh` — run the Django development server locally.
- `dev-frontend.sh` — run the React (Vite) dev server locally.

Run scripts from the repository root. See [../docs/DEVELOPMENT_WORKFLOW.md](../docs/DEVELOPMENT_WORKFLOW.md) for environment setup.
