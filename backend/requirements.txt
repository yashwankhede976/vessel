# Backend runtime dependencies (pinned). Keep minimal — no business features yet.
Django==5.0.6
djangorestframework==3.15.1
django-cors-headers==4.3.1
django-environ==0.11.2
django-filter==24.2
drf-spectacular==0.27.2
gunicorn==22.0.0
# Optional: only needed to run the live AISStream WebSocket consumer.
# The adapter imports it lazily; tests and the rest of the app do not require it.
websockets==12.0
# Mathematical optimization (multi-voyage procurement MILP). Imported lazily by
# apps.decisions.services.optimization; the rest of the app does not require it.
ortools==9.15.6755
