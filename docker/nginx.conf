# nginx config for serving the Vessel SPA.
# Falls back to index.html so client-side (React Router) routes resolve.
server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
